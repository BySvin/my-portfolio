Drop real project screenshots/demo GIFs here (e.g. `mypulse360-1.png`).

Once you have real images, replace the `PlaceholderImage` usage in `src/components/ProjectCard.tsx` with a real `next/image` referencing this folder.
